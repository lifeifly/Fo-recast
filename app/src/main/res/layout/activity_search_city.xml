<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">

    <EditText
        android:id="@+id/search_edit_city"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/search_iv_search"
        android:background="@drawable/search_bg"
        android:hint="请输入要搜索的城市"
        android:padding="3dp" />

    <ImageView
        android:id="@+id/search_iv_search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginLeft="5dp"
        android:src="@mipmap/icon_search" />

    <TextView
        android:id="@+id/search_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="热门城市"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_below="@id/search_edit_city"
        android:layout_marginTop="20dp"/>
    <GridView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/search_gv"
        android:numColumns="5"
        android:layout_below="@id/search_tv"/>

</RelativeLayout>